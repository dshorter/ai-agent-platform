"""Director runtime — Telegram switchboard, config, and the per-message run loop.

The Director's *reasoning* lives in agents/director_agent.py; this package is the
plumbing around it (capture in, reply out, log to the spine).
"""
